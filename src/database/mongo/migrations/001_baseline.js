/**
 * Migration 001 — Baseline
 *
 * This is a no-op migration that establishes the migration system.
 * It proves the runner works and serves as the version-0 marker.
 * All future migrations will be numbered sequentially after this.
 */
export async function up() {
  // No-op. Baseline migration.
}
